---
phase: 07-stage-aware-schema-foundation
plan: 06
type: execute
wave: 5
depends_on: ["07-05"]
files_modified: []
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "Prod Neon is in final stage-aware state: 6 stage columns, 0 is_actual columns, 5 stage unique keys, views recreated, defaults dropped"
    - "Prod HTML is normalized-identical across baseline -> Deploy 1, and RAW byte-identical across Deploy 1 -> post-B (same deployed build)"
    - "/search (force-dynamic, hits the recreated materialized view per-request) works throughout and after the transition — the site never errored"
    - "Migration A, Deploy 1 verification, and Migration B all completed the same day; results reported"
  artifacts:
    - path: "budget-explorer-web/.snapshots/prod-post-B/manifest.json"
      provides: "Prod post-migration snapshot proving byte-identity (gitignored; evidence recorded in SUMMARY)"
  key_links:
    - from: "git push origin (Deploy 1 commits)"
      to: "Vercel build"
      via: "prisma generate + prerender of ~70 pages against the migrated Neon DB — the build itself is the render-against-new-schema proof"
      pattern: "git push"
    - from: "pipeline/migrations/007_budget_stage_contract.sql"
      to: "prod Neon (direct endpoint)"
      via: "python -m pipeline migrate with DATABASE_URL exported to prod-direct (006 already recorded in _migrations)"
      pattern: "007_budget_stage_contract"
---

<objective>
Roll the verified expand/contract sequence out to production Neon + Vercel, same day: Migration A -> push/Deploy 1 -> snapshot verify -> Migration B -> snapshot verify. Report results.

Purpose: Locked decision — Phase 7 ships to prod immediately once local verification passes, so any stage-model problem surfaces in July with zero proposed data at stake. Standing authorization: Claude runs all prod touches via DATABASE_URL_PROD and reports results; no per-touch approval.
Output: Production fully stage-aware, byte-identity proven, rollout report in SUMMARY.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-stage-aware-schema-foundation/07-RESEARCH.md
@.planning/phases/07-stage-aware-schema-foundation/07-01-SUMMARY.md
@.planning/phases/07-stage-aware-schema-foundation/07-02-SUMMARY.md

Prod ops runbook (project CLAUDE.md, binding):
- `DATABASE_URL_PROD` lives in the root `.env` (never committed). For migrations use the DIRECT endpoint: strip `-pooler` from the Neon host.
- Export `DATABASE_URL` to the prod value before pipeline commands (load_dotenv does not override exported vars).
- Static pages have daily ISR — prod static HTML does NOT change when the DB migrates; only a deploy re-renders everything. That is why post-A static pages are EXPECTED untouched and /search (force-dynamic) is the live canary.

CRITICAL sequencing constraint discovered in planning: `python -m pipeline migrate` applies ALL pending files — running it on prod now would apply 006 AND 007 back-to-back with no deploy between. Task 1 therefore applies 006 via psql + a manual `_migrations` INSERT; Task 3 then uses the runner, which sees 006 recorded and applies only 007.

ALL THREE TASKS RUN IN ONE SESSION (locked: Migration B same day as the verified deploy).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prod pre-flight, fresh baseline, and Migration A (expand)</name>
  <files>none (prod operations; snapshots gitignored)</files>
  <action>
1. Resolve the prod base URL (NEXT_PUBLIC_CANONICAL_DOMAIN / Vercel project domain); confirm `curl -sf` on it succeeds. Build the direct (non-pooler) prod connection string: take DATABASE_URL_PROD from root .env and strip `-pooler` from the host — call it PROD_DIRECT below.
2. Pre-flight on prod (direct endpoint): re-run the pg_constraint unique-name query from 07-01 and confirm the five names match what 007 will DROP (07-01-SUMMARY expected them to match local). If ANY name differs, STOP and fix 007 before touching prod. Also record pre-A row counts + `is_actual IS TRUE` counts for the three converting tables.
3. Fresh prod baseline: `cd budget-explorer-web && node scripts/snapshot.mjs capture --base <prod-url> --out prod-baseline` (all pages must return 200).
4. Apply Migration A to prod in one transaction, recording it so the runner later skips it:
```bash
psql "$PROD_DIRECT" -v ON_ERROR_STOP=1 <<'SQL'
BEGIN;
\i pipeline/migrations/006_budget_stage_expand.sql
INSERT INTO _migrations (filename) VALUES ('006_budget_stage_expand.sql');
COMMIT;
SQL
```
   (run from repo root so the \i path resolves)
5. Post-A prod checks: stage column on all six tables; stage='actual' counts equal the recorded is_actual=true counts (compare also against 07-02-SUMMARY's local pattern); live canaries — homepage, one department page, and `/search?q=police` all still return 200 (static pages are untouched by construction; /search proves the old deployed client coexists with the expanded schema).
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer && PROD_DIRECT="$(grep -oP '(?<=^DATABASE_URL_PROD=).*' .env | tr -d '\"' | sed 's/-pooler//')" && psql "$PROD_DIRECT" -tAc "SELECT count(*) FROM information_schema.columns WHERE column_name='stage' AND table_name IN ('department_budgets','department_expenditures','revenue_by_source','millage_rates','strategic_area_budgets','budget_descriptions')" | grep -qx 6 && psql "$PROD_DIRECT" -tAc "SELECT count(*) FROM _migrations WHERE filename='006_budget_stage_expand.sql'" | grep -qx 1</automated>
  </verify>
  <done>Prod pre-flights green, prod-baseline captured, 006 applied + recorded on prod, backfill counts verified, live site healthy on all canaries.</done>
</task>

<task type="auto">
  <name>Task 2: Push Deploy 1, wait for Vercel, verify prod snapshot</name>
  <files>none (git push + prod verification)</files>
  <action>
1. Push all local Phase 7 commits: `git push origin <branch>` (the branch Vercel production deploys from — confirm with `git remote show origin` / vercel project settings). This is the FIRST push of the phase; prod Neon now has the stage column, so the Vercel build (prisma generate from the final-state schema + prerender of ~70 pages against Neon) must succeed. The build itself is the render-against-new-schema proof (RESEARCH.md Pitfall 4).
2. Wait for the deployment to go live: poll with `vercel ls` / `gh` deployment status, or poll the site until the build id in page HTML changes. If the build FAILS, the site keeps serving the previous deployment (no user impact) — diagnose, fix, re-push.
3. Capture `prod-deploy1`: `node scripts/snapshot.mjs capture --base <prod-url> --out prod-deploy1`.
4. `node scripts/snapshot.mjs diff prod-baseline prod-deploy1 --normalize` — must be identical (cross-build: Tier 1). This is the locked full-crawl gate on prod for the code swap.
5. Canary: `/search?q=police` returns 200 with results (new client + dual-column DB).
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && node scripts/snapshot.mjs diff prod-baseline prod-deploy1 --normalize</automated>
  </verify>
  <done>Deploy 1 live on Vercel; full prod crawl normalized-identical to the pre-migration prod baseline; /search healthy.</done>
</task>

<task type="auto">
  <name>Task 3: Prod Migration B (same day), final byte-identity proof, rollout report</name>
  <files>none (prod operations)</files>
  <action>
SAME DAY as Task 2's verified deploy (locked decision — the dual-column window lasts hours, not days):

1. Apply 007 via the runner against prod direct: `export DATABASE_URL="$PROD_DIRECT"` then `python -m pipeline migrate` — output must show `SKIP 006` and `APPLY 007_budget_stage_contract.sql` only. (Runner wraps the file in a transaction; a constraint-name mismatch aborts cleanly with nothing half-applied.)
2. Schema state assertion on prod: 0 is_actual columns anywhere, 6 stage columns, 5 `_stage_key` constraints, 0 stage column defaults, `search_index` populated (CREATE populates WITH DATA — no separate REFRESH needed), `v_department_yoy` queryable.
3. Capture `prod-post-B`: `node scripts/snapshot.mjs capture --base <prod-url> --out prod-post-B`.
4. `node scripts/snapshot.mjs diff prod-deploy1 prod-post-B` — RAW Tier 0, no --normalize: same deployed build on both sides, so this is the true byte-for-byte gate. Must exit 0.
5. Exercise `/search?q=police` and one more query — proves the recreated materialized view serves per-request traffic. No redeploy needed (deployed code never references is_actual; ISR re-renders within 24h run against the final schema — Deploy 1's build already proved those queries).
6. Unset the DATABASE_URL override. Report in the SUMMARY: timestamps of A / Deploy 1 / B (proving same-day), per-table backfill counts local vs prod, all snapshot gate results, canary results. Per standing authorization, results are reported — this report is the deliverable Diego reviews.
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer && PROD_DIRECT="$(grep -oP '(?<=^DATABASE_URL_PROD=).*' .env | tr -d '\"' | sed 's/-pooler//')" && psql "$PROD_DIRECT" -tAc "SELECT count(*) FROM information_schema.columns WHERE column_name='is_actual'" | grep -qx 0 && psql "$PROD_DIRECT" -tAc "SELECT count(*) FROM pg_constraint WHERE conname LIKE '%_stage_key'" | grep -qx 5 && psql "$PROD_DIRECT" -tAc "SELECT count(*) > 0 FROM search_index" | grep -qx t && cd budget-explorer-web && node scripts/snapshot.mjs diff prod-deploy1 prod-post-B</automated>
  </verify>
  <done>Prod contracted same day: is_actual gone, stage keys live, MV serving; prod-post-B raw byte-identical to prod-deploy1; rollout fully reported.</done>
</task>

</tasks>

<verification>
- Prod schema assertion: 6 stage / 0 is_actual / 5 stage keys / 0 defaults; `_migrations` has 006 and 007
- `diff prod-baseline prod-deploy1 --normalize` exit 0; `diff prod-deploy1 prod-post-B` (raw) exit 0
- /search healthy at every stage of the transition (never-errors requirement)
- Same-day timestamps for A, Deploy 1 verification, and B recorded in SUMMARY
</verification>

<success_criteria>
- Production is fully stage-aware with zero user-visible change, mechanically proven by the full-crawl gates
- The public site never errored during the transition
- Phase 8 can start against a schema where stale is_actual references are impossible on both databases
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-aware-schema-foundation/07-06-SUMMARY.md` — include the full rollout report (timestamps, counts, gate results) per the standing-authorization reporting requirement.
</output>
