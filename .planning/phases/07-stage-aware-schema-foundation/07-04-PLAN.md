---
phase: 07-stage-aware-schema-foundation
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - pipeline/load/seed.py
  - pipeline/load/seed_historical.py
  - pipeline/load/seed_descriptions.py
  - pipeline/verify/checker.py
  - pipeline/generate/descriptions.py
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "All three seeders write an explicit stage on every INSERT, use stage-based ON CONFLICT targets, and stage-scope their DELETEs"
    - "checker.py and generate/descriptions.py read stage='adopted' instead of is_actual=FALSE, and python -m pipeline verify passes against the post-A local DB"
    - "The only is_actual references left in the pipeline are CSV-field parsing (transform/historical.py + the single bool-to-enum mapping in seed_historical.py)"
  artifacts:
    - path: "pipeline/load/seed.py"
      provides: "stage-aware current-FY seeder (stage='adopted' threaded through seed_all)"
      contains: "stage"
    - path: "pipeline/load/seed_historical.py"
      provides: "historical seeder mapping CSV is_actual bool to 'actual'/'adopted' at the seed layer"
    - path: "pipeline/verify/checker.py"
      provides: "verification queries scoped to stage='adopted'"
      contains: "stage = 'adopted'"
  key_links:
    - from: "pipeline/load/seed.py"
      to: "millage_rates_fy_authority_stage_key"
      via: "ON CONFLICT (fiscal_year_id, authority, stage) — constraint exists only after 007 (plan 07-05); loaders must NOT run during the dual-column window"
      pattern: "fiscal_year_id, authority, stage"
    - from: "pipeline/verify/checker.py"
      to: "local post-A database"
      via: "python -m pipeline verify"
      pattern: "stage = 'adopted'"
---

<objective>
Rewrite all pipeline writers and readers to speak stage instead of is_actual, so the pipeline is fully stage-native before Migration B drops the boolean.

Purpose: RESEARCH.md Pitfall 7 — if these rewrites don't land before Migration B, `python -m pipeline load` crashes post-B and Phase 8 starts on broken loaders. Written to FINAL (stage-only) state, not dual-write: no pipeline load runs during the dual-column window, is_actual is nullable-with-default so nothing can violate it, and the stage-based conflict targets these files reference come into existence with 007.
Output: Five stage-native pipeline files, committed locally.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-stage-aware-schema-foundation/07-RESEARCH.md
@pipeline/load/seed.py
@pipeline/load/seed_historical.py
@pipeline/load/seed_descriptions.py
@pipeline/verify/checker.py
@pipeline/generate/descriptions.py

HARD RULE for this plan and until 07-05 completes: do NOT run `python -m pipeline load`, `run-all`, `seed-historical`, or `seed_descriptions` — the stage-based ON CONFLICT targets written here reference unique constraints that Migration B (007) creates. Running a loader mid-window errors on the conflict target. `python -m pipeline verify` and `python -m pipeline migrate` are safe.

Runs in Wave 3 in parallel with 07-03 (web). Zero file overlap; both depend only on Migration A being applied locally (07-02) so the stage column exists for checker queries.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the three seeders to stage-native writes</name>
  <files>pipeline/load/seed.py, pipeline/load/seed_historical.py, pipeline/load/seed_descriptions.py</files>
  <action>
`pipeline/load/seed.py` (RESEARCH.md Pattern 2, table 3):
- Thread a `stage: str = 'adopted'` parameter through `seed_all` and down to the seeding functions (Phase 8 will call with 'proposed'; default keeps current CLI behavior identical).
- The 4 INSERTs that write `is_actual` FALSE (~lines 127, 273, 301, 424): replace the is_actual column/value with `stage` from the parameter.
- Millage upsert (~line 481): change `ON CONFLICT (fiscal_year_id, authority)` to `ON CONFLICT (fiscal_year_id, authority, stage)`.
- The 5 FY-scoped DELETEs: add `AND stage = %s` with the stage parameter. Byte-invisible today (all rows adopted) but this is the Phase 13 September-wipe protection — a stage-scoped delete can never wipe proposed rows when loading adopted data (and vice versa).

`pipeline/load/seed_historical.py`:
- Keep `transform/historical.py` and the CSV files UNCHANGED — the CSV column keeps its `is_actual` name; only the loader maps it.
- At the seed layer, map the record's bool: `stage = 'actual' if record['is_actual'] else 'adopted'` — this is the ONE legitimate remaining is_actual reference in pipeline/load/.
- INSERT (~line 220): write `stage` instead of is_actual; `ON CONFLICT` target becomes `(fiscal_year_id, department_id, strategic_area_id, stage)`.
- `seen_keys` tuple (~line 204): swap the is_actual element for the mapped stage value.
- The FY-scoped DELETE: add stage scoping consistent with the mapped value.

`pipeline/load/seed_descriptions.py`:
- DELETE and INSERT (~lines 93-114): both gain explicit `stage = 'adopted'` (literal, not parameter — descriptions regenerate per stage in Phase 13 and will be parameterized then; scope lock says literal now).

Never rely on the transitional DB default — every INSERT states stage explicitly (Migration B drops the defaults precisely so silent adopted-writes become impossible; Pitfall 6).
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer && python -m py_compile pipeline/load/seed.py pipeline/load/seed_historical.py pipeline/load/seed_descriptions.py && ! grep -q is_actual pipeline/load/seed.py pipeline/load/seed_descriptions.py && test "$(grep -c is_actual pipeline/load/seed_historical.py)" -le 3 && grep -q "fiscal_year_id, authority, stage" pipeline/load/seed.py</automated>
  </verify>
  <done>All three seeders compile; seed.py and seed_descriptions.py have zero is_actual references; seed_historical.py retains only the CSV-record bool-to-enum mapping; conflict targets are stage-based; every INSERT states stage explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Convert pipeline readers (checker, descriptions generator) and prove verify green</name>
  <files>pipeline/verify/checker.py, pipeline/generate/descriptions.py</files>
  <action>
`pipeline/verify/checker.py` — 4 sites (~lines 61, 100, 150, 180): replace `is_actual = FALSE` predicates with `stage = 'adopted'`.

`pipeline/generate/descriptions.py` — 2 sites (~lines 82, 99): replace `is_actual = FALSE` with `stage = 'adopted'`.

Keep query semantics otherwise identical (same joins, same COALESCE area-membership logic where present — matches migration 003 views).

Then run `python -m pipeline verify` against the local post-A DB: the stage column exists and is backfilled, so the stage-scoped checker must reproduce exactly the same green result the is_actual checker gave in 07-02. This is the proof the pipeline speaks stage correctly.

Commit the plan's changes locally (e.g. `feat(07-04): stage-native pipeline loaders and readers`). Do NOT push (push happens in 07-06 after prod Migration A).
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer && ! grep -q is_actual pipeline/verify/checker.py pipeline/generate/descriptions.py && python -m pipeline verify && git log --oneline -1 | grep -q "07-04"</automated>
  </verify>
  <done>Zero is_actual in checker/generator; python -m pipeline verify green against the post-A local DB via stage queries; committed locally, not pushed.</done>
</task>

</tasks>

<verification>
- `python -m pipeline verify` green (stage-scoped checker, post-A DB)
- `grep -rn is_actual pipeline/ --include='*.py'` shows only transform/historical.py plus the single mapping in seed_historical.py
- All files compile; no loader was executed during the dual-column window
</verification>

<success_criteria>
- Pipeline is fully stage-native: writers state stage explicitly, readers filter it explicitly
- Post-Migration-B, `python -m pipeline load` will work unchanged (conflict targets match 007's constraints)
- The September stage-scoped-delete landmine (Phase 13 dress rehearsal) is already defused
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-aware-schema-foundation/07-04-SUMMARY.md`.
</output>
