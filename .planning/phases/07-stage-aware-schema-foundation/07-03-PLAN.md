---
phase: 07-stage-aware-schema-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - budget-explorer-web/prisma/schema.prisma
  - budget-explorer-web/src/lib/db/queries.ts
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "schema.prisma is in FINAL shape: budget_stage enum, stage field on all six models, is_actual fields DELETED (even though DB columns still exist), stage-based @@unique blocks with the exact map names Migration B will create"
    - "All 10 reader touch points in queries.ts filter stage='adopted'; getDepartmentYoY handles all three enum members explicitly and skips 'proposed'"
    - "tsc --noEmit and vitest are green against the regenerated client; zero is_actual references remain in web source"
    - "Built site HTML is normalized-identical to the pre-migration baseline (the code swap is invisible)"
  artifacts:
    - path: "budget-explorer-web/prisma/schema.prisma"
      provides: "Deploy 1 contract: final-state schema the Vercel build generates the client from"
      contains: "enum budget_stage"
    - path: "budget-explorer-web/src/lib/db/queries.ts"
      provides: "All reader queries stage-scoped to 'adopted'"
      contains: "stage: 'adopted'"
  key_links:
    - from: "budget-explorer-web/src/lib/db/queries.ts"
      to: "generated Prisma budget_stage enum"
      via: "stage filters typed by the regenerated client"
      pattern: "stage: 'adopted'"
    - from: "budget-explorer-web/prisma/schema.prisma"
      to: "pipeline/migrations/007_budget_stage_contract.sql"
      via: "@@unique map: names must equal the constraints 007 creates (db-pull no-diff gate in 07-05 depends on this)"
      pattern: "_stage_key"
---

<objective>
Produce the web half of Deploy 1: hand-write the final-state Prisma schema (is_actual absent, stage present) and convert every reader query to explicit `stage: 'adopted'` filters — then prove the code swap renders byte-identically.

Purpose: The committed schema.prisma is the deploy contract — Vercel runs `prisma generate` from the file, never the DB. With is_actual absent from the schema, Migration B's column drop provably cannot break the running site (RESEARCH.md Pitfall 1). The six newly-added stage filters are what make success criterion 3 (proposed-row invisibility) achievable at all (Pitfall 5).
Output: Deploy-1-ready web code, committed locally. NOT pushed.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-stage-aware-schema-foundation/07-RESEARCH.md
@budget-explorer-web/prisma/schema.prisma
@budget-explorer-web/src/lib/db/queries.ts

CRITICAL SEQUENCING: commit locally only. DO NOT push to GitHub in this plan — a push triggers a Vercel build that prerenders against prod Neon, which does not have the stage column until plan 07-06 runs Migration A there. The push happens in 07-06 Task 2, after prod Migration A.

DO NOT run `prisma db pull` in this plan (RESEARCH.md Pitfall 8): mid-window it would resurrect is_actual and the old unique keys. db pull runs exactly once, after Migration B, as a no-diff verification (07-05).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-write final-state schema.prisma and regenerate the client</name>
  <files>budget-explorer-web/prisma/schema.prisma</files>
  <action>
Edit `prisma/schema.prisma` to the FINAL post-Migration-B shape (hand-written, not db-pulled — the DB is mid-window and db pull would re-add is_actual):

1. Add the enum (introspection-shaped, lifecycle order):
```prisma
enum budget_stage {
  proposed
  adopted
  actual
}
```
2. On all six models — `department_budgets`, `department_expenditures`, `revenue_by_source`, `millage_rates`, `strategic_area_budgets`, `budget_descriptions`:
   - Add `stage budget_stage` (no @default — Migration B drops the DB defaults; write it exactly as db pull will emit post-B). Place the field LAST in each model (db pull emits fields in DB column order; stage was added last).
3. On the three converting models: DELETE the `is_actual Boolean? @default(false)` field entirely. Prisma tolerates DB columns absent from the schema — during the dual-column window the client simply never touches is_actual.
4. Swap the @@unique blocks to stage, with map: names EXACTLY matching what 007 will create (these names are the contract with plan 07-05):
   - department_budgets: `@@unique([fiscal_year_id, department_id, strategic_area_id, stage], map: "department_budgets_fy_dept_area_stage_key")`
   - department_expenditures: `@@unique([fiscal_year_id, department_id, expenditure_category_id, stage], map: "department_expenditures_fy_dept_cat_stage_key")`
   - revenue_by_source: `@@unique([fiscal_year_id, revenue_source_id, stage], map: "revenue_by_source_fy_source_stage_key")`
   - millage_rates: `@@unique([fiscal_year_id, authority, stage], map: "millage_rates_fy_authority_stage_key")`
   - strategic_area_budgets: `@@unique([fiscal_year_id, strategic_area_id, stage], map: "strategic_area_budgets_fy_area_stage_key")`
   - budget_descriptions: NO unique key (it has none today; introducing one is a behavior change outside zero-change scope).

The @@unique/DB mismatch during the window is harmless: the app only uses findMany/findFirst/count/$queryRaw, never compound-key findUnique.

Then `cd budget-explorer-web && pnpm exec prisma generate`.
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && pnpm exec prisma generate && ! grep -q is_actual prisma/schema.prisma && grep -q "enum budget_stage" prisma/schema.prisma && grep -c "_stage_key" prisma/schema.prisma | grep -qx 5</automated>
  </verify>
  <done>schema.prisma has zero is_actual references, the budget_stage enum, stage on six models, five stage-keyed @@unique blocks with 007's exact constraint names; client regenerates cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Convert all 11 queries.ts touch points to explicit stage filters</name>
  <files>budget-explorer-web/src/lib/db/queries.ts</files>
  <action>
Apply the complete reader inventory (RESEARCH.md Pattern 2 — 10 filter sites + the YoY logic; line numbers are pre-edit references):

Converting existing filters (is_actual: false -> stage: 'adopted'):
- `getAreaWithDepartments` department_budgets filter (~line 191)
- `getDepartmentDetail` department_budgets filter (~line 321)
- `getRelatedDepartments` department_budgets filter (~line 404)

ADDING filters where none exist today (byte-invisible now — every row is adopted or already-excluded actual — but REQUIRED for the proposed-row probe and Phase 8 safety):
- `getRevenueSources` revenue_by_source (~line 250): add `stage: 'adopted'`
- `getMillageRates` millage_rates (~line 96): add `stage: 'adopted'`
- `getDepartmentExpenditures` department_expenditures (~line 370): add `stage: 'adopted'`
- `getStrategicAreas` strategic_area_budgets include (~line 121): add `stage: 'adopted'`
- `getAreaWithDepartments` strategic_area_budgets include (~line 175): add `stage: 'adopted'`
- `getStrategicAreasWithDetails` strategic_area_budgets include (~line 282): add `stage: 'adopted'`
- `getDepartmentDetail` budget_descriptions lookup (~line 327): add `stage: 'adopted'`

getDepartmentYoY (~line 464) — the one non-mechanical edit. Replace the boolean ternary with explicit three-way handling so Phase 8 proposed rows can never leak into history charts:
```typescript
// today:  const byYear = b.is_actual ? actual : adopted
if (b.stage === 'proposed') continue
const byYear = b.stage === 'actual' ? actual : adopted
```
Also move the `yearLabels` push (~line 461) BELOW the proposed-skip so a proposed-only year never registers a label, and update the stale is_actual comments (~lines 452-453) to describe stage semantics.

Leave untouched: `sumBudgetRows()`, `areaMembershipFilter()`, `searchBudget` (the MV pins stage internally post-007), and sitemap.ts (reads no stage columns).
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && pnpm exec tsc --noEmit && pnpm exec vitest run && ! grep -rq is_actual src/lib/db/queries.ts && grep -c "stage: 'adopted'" src/lib/db/queries.ts | grep -qx 10</automated>
  </verify>
  <done>tsc zero errors against the regenerated client (the type system confirms no stale is_actual site can exist), vitest green, exactly 10 stage:'adopted' filters present, YoY three-way handling in place.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the code swap is invisible — deploy1 snapshot vs baseline; commit locally</name>
  <files>budget-explorer-web/.snapshots/local-deploy1/ (gitignored output)</files>
  <action>
1. `cd budget-explorer-web && pnpm build` (new code, local post-A DB).
2. `pnpm start` in background, wait for ready.
3. `node scripts/snapshot.mjs capture --base http://localhost:3000 --out local-deploy1`
4. `node scripts/snapshot.mjs diff local-baseline local-deploy1 --normalize` — must be identical (cross-build comparison; Tier 1). This proves the entire Deploy 1 code change renders every one of the ~70 pages byte-identically.
5. Stop the server.
6. Commit the plan's changes locally (conventional commit, e.g. `feat(07-03): stage-aware schema.prisma and reader queries`). DO NOT push — prod Neon has no stage column until 07-06.

If any page differs: the mismatch report shows the page and lines — fix the responsible query change and re-run. Do not proceed red.
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && node scripts/snapshot.mjs diff local-baseline local-deploy1 --normalize && cd /home/d48reu/BudgetExplorer && git log --oneline -1 | grep -q "07-03" && test -z "$(git status --porcelain budget-explorer-web/prisma budget-explorer-web/src)"</automated>
  </verify>
  <done>local-deploy1 crawl normalized-identical to the pre-migration baseline; changes committed locally; nothing pushed.</done>
</task>

</tasks>

<verification>
- `grep is_actual` returns nothing in budget-explorer-web/src and prisma/
- `pnpm exec tsc --noEmit`, `pnpm exec vitest run` green
- `node scripts/snapshot.mjs diff local-baseline local-deploy1 --normalize` exits 0
- No push to origin occurred (Vercel must not build yet)
</verification>

<success_criteria>
- Deploy 1 web contract complete: final-state schema + 10 stage filters + three-way YoY, all proven byte-invisible
- Migration B is now provably unable to break the site: no code path references is_actual
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-aware-schema-foundation/07-03-SUMMARY.md`.
</output>
