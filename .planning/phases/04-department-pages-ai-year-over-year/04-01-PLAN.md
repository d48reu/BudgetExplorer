---
phase: 04-department-pages-ai-year-over-year
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/generate/__init__.py
  - pipeline/generate/descriptions.py
  - pipeline/load/seed_descriptions.py
  - pipeline/data/descriptions/.gitkeep
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
user_setup:
  - service: anthropic
    why: "AI description generation for 35 departments via Claude API"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (https://console.anthropic.com/settings/keys)"
    dashboard_config: []

must_haves:
  truths:
    - "Running `python -m pipeline.generate.descriptions` produces a JSON file with 35 department entries, each containing summary, detailed_description, and key_changes fields"
    - "Running `python -m pipeline.load.seed_descriptions` reads the reviewed JSON and inserts all 35 descriptions into the budget_descriptions table"
    - "Each seeded description record includes fiscal_year_id, entity_type='department', entity_id, generated_at timestamp, and model_version"
    - "Generated descriptions use civic plain-English tone with specific dollar amounts and employee counts"
  artifacts:
    - path: "pipeline/generate/descriptions.py"
      provides: "AI description batch generation script"
      min_lines: 80
    - path: "pipeline/load/seed_descriptions.py"
      provides: "Description database seeding from reviewed JSON"
      min_lines: 40
    - path: "pipeline/data/descriptions/fy_2025_26_descriptions.json"
      provides: "Generated descriptions for human review"
  key_links:
    - from: "pipeline/generate/descriptions.py"
      to: "Anthropic API"
      via: "anthropic.Anthropic().messages.create"
      pattern: "client\\.messages\\.create"
    - from: "pipeline/load/seed_descriptions.py"
      to: "budget_descriptions table"
      via: "psycopg2 INSERT"
      pattern: "INSERT INTO budget_descriptions"
---

<objective>
Create the Python AI description pipeline that generates plain-English summaries for all 35 Miami-Dade County departments using the Anthropic Claude API, saves them as JSON for human review, and seeds reviewed descriptions into the database.

Purpose: AI-generated descriptions make department budgets accessible to residents who have never read a budget document. Pre-generating and reviewing ensures quality and avoids runtime API costs.
Output: Two Python scripts (generate + seed) and a JSON output file ready for human review.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-department-pages-ai-year-over-year/04-CONTEXT.md
@.planning/phases/04-department-pages-ai-year-over-year/04-RESEARCH.md

@pipeline/config.py
@pipeline/load/db.py
@pipeline/load/seed.py

<interfaces>
<!-- Existing pipeline infrastructure the executor needs -->

From pipeline/config.py:
```python
DATABASE_URL = os.getenv("DATABASE_URL", "postgresql://...")
CURRENT_FISCAL_YEAR = "FY 2025-26"
```

From pipeline/load/db.py:
```python
def get_db_connection():
    """Context manager returning psycopg2 connection with auto-commit/rollback."""
```

From budget_descriptions table (Prisma schema):
```
model budget_descriptions {
  id                   Int          @id @default(autoincrement())
  fiscal_year_id       Int
  entity_type          String       @db.VarChar(50)
  entity_id            Int
  summary              String
  detailed_description String?
  key_changes          String?
  generated_at         DateTime?    @default(now()) @db.Timestamptz(6)
  model_version        String?      @db.VarChar(50)
}
```

From v_department_yoy SQL view (provides YoY data for key_changes context):
```sql
CREATE VIEW v_department_yoy AS
SELECT d.name AS department, d.slug, sa.name AS strategic_area,
       fy.label AS fiscal_year, db.operating_budget, db.capital_budget,
       db.employee_count,
       LAG(db.operating_budget) OVER (PARTITION BY d.id ORDER BY fy.start_date) AS prior_operating,
       LAG(db.employee_count) OVER (PARTITION BY d.id ORDER BY fy.start_date) AS prior_employees
FROM department_budgets db
JOIN departments d ON d.id = db.department_id
JOIN strategic_areas sa ON sa.id = d.strategic_area_id
JOIN fiscal_years fy ON fy.id = db.fiscal_year_id
WHERE db.is_actual = FALSE
ORDER BY d.name, fy.start_date;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI description generation and seeding scripts</name>
  <files>pipeline/generate/__init__.py, pipeline/generate/descriptions.py, pipeline/load/seed_descriptions.py, pipeline/data/descriptions/.gitkeep</files>
  <action>
**1. Create `pipeline/generate/__init__.py`** (empty file to make it a package).

**2. Create `pipeline/generate/descriptions.py`** with these specifics:

- Import `anthropic`, `pydantic`, `json`, `os`, `time`, and project modules (`pipeline.config`, `pipeline.load.db`).
- Define a Pydantic model `DepartmentDescription` with fields: `summary: str`, `detailed_description: str`, `key_changes: str`.
- Define `SYSTEM_PROMPT` (per CONTEXT.md locked decision):
  ```
  You are a civic communications writer for Miami-Dade County.
  Write in plain English that any resident can understand -- no jargon, no acronyms, no budget-speak.
  Think "county newsletter" tone.

  Rules:
  - Use specific dollar amounts (e.g., "$1.2 billion") and employee counts
  - Never use words like: leverage, synergy, optimize, stakeholder, utilize, facilitate, enhance
  - Write as if explaining to a neighbor who has never seen a budget document
  - Be factual and neutral -- no political commentary or value judgments
  - Reference the fiscal year in descriptions (e.g., "In FY 2025-26...")
  ```
- Define `fetch_department_data(conn)` that queries all departments with their current FY budget, employee count, strategic area name, and YoY data from `v_department_yoy` (filtering for current FY). Returns list of dicts.
- Define `generate_description(client, dept_data: dict) -> DepartmentDescription` that:
  - Builds a user prompt with department name, strategic area, operating budget (converted from cents to dollars), capital budget, employee count, and YoY changes (prior operating, prior employees)
  - Calls `client.messages.create()` with model `"claude-sonnet-4-5-20250929"`, max_tokens=1024, system=SYSTEM_PROMPT
  - Parses the text response into `DepartmentDescription` using a structured prompt that asks Claude to return JSON with the 3 fields
  - Falls back to `json.loads()` extraction from the response text
  - Note: Do NOT use `messages.parse()` -- use `messages.create()` with a JSON-formatted prompt, then parse the response text. The `messages.parse()` method may not be available in all SDK versions.
- Define `main()` function that:
  - Creates Anthropic client (uses ANTHROPIC_API_KEY from env automatically)
  - Fetches all department data from DB
  - Iterates over departments, calling `generate_description` for each with a 1-second delay between calls
  - Prints progress (e.g., "[3/35] Generating for Fire Rescue...")
  - Saves output to `pipeline/data/descriptions/fy_2025_26_descriptions.json` as a dict keyed by department slug, with each value containing summary, detailed_description, key_changes, plus metadata (department_name, department_id, strategic_area, fiscal_year, model_version, generated_at ISO timestamp)
  - Handles errors per-department (logs error, continues to next, marks failed ones)
- Add `if __name__ == "__main__": main()` and also make it callable via `python -m pipeline.generate.descriptions`.

**3. Create `pipeline/load/seed_descriptions.py`:**
- Import `json`, `os`, `sys`, and project modules (`pipeline.config`, `pipeline.load.db`).
- Define `seed_descriptions(json_path: str)` that:
  - Reads the JSON file
  - Gets DB connection via `get_db_connection()`
  - Looks up current fiscal_year_id from `fiscal_years` table WHERE label = CURRENT_FISCAL_YEAR
  - For each department in JSON:
    - Looks up department_id from `departments` table WHERE slug = dept_slug
    - Deletes existing description (idempotent): `DELETE FROM budget_descriptions WHERE fiscal_year_id = %s AND entity_type = 'department' AND entity_id = %s`
    - Inserts new description: `INSERT INTO budget_descriptions (fiscal_year_id, entity_type, entity_id, summary, detailed_description, key_changes, generated_at, model_version) VALUES (%s, 'department', %s, %s, %s, %s, %s, %s)`
  - Commits transaction
  - Prints count of seeded descriptions
- Add `if __name__ == "__main__":` block that takes optional JSON path argument (defaults to `pipeline/data/descriptions/fy_2025_26_descriptions.json`).

**4. Create `pipeline/data/descriptions/.gitkeep`** (empty file to ensure directory exists in git).

**5. Install Python dependencies** if not already present:
```bash
pip install anthropic pydantic
```
Update `requirements.txt` to include `anthropic` and `pydantic` if not already listed.
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer && python -c "from pipeline.generate.descriptions import fetch_department_data; print('Import OK')" && python -c "from pipeline.load.seed_descriptions import seed_descriptions; print('Import OK')"</automated>
  </verify>
  <done>Both scripts exist, import cleanly, and follow the pipeline patterns (config, db connection, idempotent seeding). Generation script has Pydantic model, system prompt with civic tone rules, and per-department error handling. Seed script does DELETE-then-INSERT pattern matching existing seed.py.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Generate, review, and seed AI descriptions</name>
  <files>pipeline/data/descriptions/fy_2025_26_descriptions.json</files>
  <action>
Human must run the AI generation pipeline and review output before seeding:

1. Ensure ANTHROPIC_API_KEY is set in `.env` file
2. Run generation: `python -m pipeline.generate.descriptions` (~2 min, ~$0.50 with Claude Sonnet)
3. Review output at `pipeline/data/descriptions/fy_2025_26_descriptions.json` -- check tone, accuracy, edit if needed
4. Run seeding: `python -m pipeline.load.seed_descriptions`
5. Verify: `SELECT COUNT(*) FROM budget_descriptions WHERE entity_type = 'department';` should return 35
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer && python -c "import psycopg2; from pipeline.config import DATABASE_URL; conn = psycopg2.connect(DATABASE_URL); cur = conn.cursor(); cur.execute(\"SELECT COUNT(*) FROM budget_descriptions WHERE entity_type = 'department'\"); print(f'Descriptions: {cur.fetchone()[0]}'); conn.close()"</automated>
  </verify>
  <done>35 department descriptions exist in budget_descriptions table with summary, detailed_description, key_changes, generated_at, and model_version populated. Descriptions use civic plain-English tone.</done>
</task>

</tasks>

<verification>
- `pipeline/generate/descriptions.py` exists with Anthropic SDK integration and Pydantic model
- `pipeline/load/seed_descriptions.py` exists with idempotent seeding logic
- Both scripts follow existing pipeline patterns (config.py, db.py context manager)
- System prompt enforces civic plain-English tone per CONTEXT.md locked decision
- Generated JSON includes all required metadata (fiscal_year, generated_at, model_version)
- After human runs pipeline: `SELECT COUNT(*) FROM budget_descriptions WHERE entity_type = 'department'` returns 35
</verification>

<success_criteria>
1. Generation script produces valid JSON with 35 department entries, each having summary (2-3 sentences), detailed_description (1-2 paragraphs), and key_changes fields
2. Seed script inserts all 35 descriptions into budget_descriptions table with correct fiscal_year_id, entity_type, entity_id, generated_at, and model_version
3. Descriptions use civic plain-English tone with specific dollar amounts -- no jargon
4. Pipeline is idempotent (re-running seed replaces existing descriptions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-department-pages-ai-year-over-year/04-01-SUMMARY.md`
</output>
