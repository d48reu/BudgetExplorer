---
phase: 02-app-foundation-design-system
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - budget-explorer-web/src/components/homepage/QuickStats.tsx
autonomous: true
requirements:
  - UX-04
  - PAGE-01
  - UX-01
  - UX-02
  - UX-03
  - UX-05
  - UX-06
  - UX-07
  - SEO-03
gap_closure: true

must_haves:
  truths:
    - "At least one budget jargon term on the homepage is wrapped in <BudgetTerm> and shows a tooltip on hover/focus"
    - "The 'Strategic Areas' label in QuickStats displays a dotted underline indicating it has a tooltip explanation"
  artifacts:
    - path: "budget-explorer-web/src/components/homepage/QuickStats.tsx"
      provides: "BudgetTerm usage wrapping 'Strategic Areas' label"
      contains: "BudgetTerm"
  key_links:
    - from: "budget-explorer-web/src/components/homepage/QuickStats.tsx"
      to: "budget-explorer-web/src/components/ui/BudgetTerm.tsx"
      via: "import { BudgetTerm }"
      pattern: "import.*BudgetTerm.*from"
    - from: "budget-explorer-web/src/components/homepage/QuickStats.tsx"
      to: "budget-explorer-web/src/lib/glossary.ts"
      via: "import { GLOSSARY_TERMS }"
      pattern: "import.*GLOSSARY_TERMS.*from"
---

<objective>
Wire the orphaned BudgetTerm tooltip component into the homepage by wrapping "Strategic Areas" in QuickStats.tsx with a `<BudgetTerm>` element.

Purpose: Close verification gap — BudgetTerm.tsx is fully implemented with Floating UI but has zero JSX usages in the entire src/ tree. UX-04 ("no jargon without tooltip/explanation") requires at least one rendered tooltip on the homepage.

Output: QuickStats.tsx renders "Strategic Areas" inside a `<BudgetTerm>` with the glossary definition, producing a dotted underline that shows a plain-English tooltip on hover/focus.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-foundation-design-system/02-02-SUMMARY.md
@.planning/phases/02-app-foundation-design-system/02-03-SUMMARY.md
@budget-explorer-web/src/components/homepage/QuickStats.tsx
@budget-explorer-web/src/components/ui/BudgetTerm.tsx
@budget-explorer-web/src/lib/glossary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire BudgetTerm into QuickStats for "Strategic Areas" label</name>
  <files>budget-explorer-web/src/components/homepage/QuickStats.tsx</files>
  <action>
Modify QuickStats.tsx to wire the orphaned BudgetTerm tooltip component:

1. Add `'use client'` directive at line 1. QuickStats currently has no `'use client'` directive because it only renders props — no async calls or server-only APIs. Adding `'use client'` is safe since it receives all data via props from the Server Component page.tsx.

2. Import `BudgetTerm` from `@/components/ui/BudgetTerm` and `GLOSSARY_TERMS` from `@/lib/glossary`.

3. At the top of the component body, look up the Strategic Area glossary entry:
   ```typescript
   const strategicAreaTerm = GLOSSARY_TERMS.find((t) => t.term === 'Strategic Area')
   ```

4. In the stats array, change the 'Strategic Areas' entry to use a JSX element for the label instead of a plain string. Update the stats array to use `React.ReactNode` for labels. Replace the plain string `'Strategic Areas'` label with a JSX expression that wraps it in `<BudgetTerm>`:
   ```tsx
   {
     value: strategicAreaCount.toLocaleString('en-US'),
     label: strategicAreaTerm ? (
       <BudgetTerm term={strategicAreaTerm.term} definition={strategicAreaTerm.definition}>
         Strategic Areas
       </BudgetTerm>
     ) : (
       'Strategic Areas'
     ),
     key: 'strategic-areas',
   }
   ```

5. Since labels are now `React.ReactNode` (not just strings), add a `key` field to each stat object and use it as the React key in the `.map()` instead of `stat.label` (which may be a JSX element). Update the type of the stats array from implicit `{ value: string; label: string }[]` to `{ value: string; label: React.ReactNode; key: string }[]`. Other stat entries keep plain string labels and get key values like `'departments'`, `'employees'`, `'budget-year'`.

6. Update the `.map()` call to use `stat.key` as the React key: `stats.map((stat) => (<Card key={stat.key} ...>`.

Do NOT change: the overall grid layout, the Card component usage, the section aria-label, or any other stat entries beyond adding `key` fields. Keep the component minimal — only add what's needed for the BudgetTerm wiring.
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && npx next build 2>&1 | tail -20</automated>
    <manual>Load homepage at http://localhost:3000. "Strategic Areas" stat card should show dotted underline. Hover or tab-focus should display tooltip with "Strategic Area" term and definition.</manual>
    <sampling_rate>run after this task commits, before summary</sampling_rate>
  </verify>
  <done>"Strategic Areas" label in QuickStats is wrapped in BudgetTerm with the glossary definition. The component renders a dotted underline and shows a Floating UI tooltip on hover/focus. Build passes with no errors. At least one <BudgetTerm> usage exists in the rendered JSX tree, satisfying UX-04.</done>
</task>

</tasks>

<verification>
- `grep -r "BudgetTerm" budget-explorer-web/src/ --include="*.tsx" -l` returns at least two files: BudgetTerm.tsx itself AND QuickStats.tsx
- `grep "import.*BudgetTerm" budget-explorer-web/src/components/homepage/QuickStats.tsx` confirms the import exists
- `grep "GLOSSARY_TERMS" budget-explorer-web/src/components/homepage/QuickStats.tsx` confirms glossary data is used
- `npx next build` completes without errors
</verification>

<success_criteria>
- BudgetTerm component is no longer orphaned — at least one JSX usage exists in the rendered page tree
- "Strategic Areas" on the homepage QuickStats shows a dotted underline indicating tooltip availability
- Hovering/focusing the term displays the glossary definition in a Floating UI tooltip
- Build passes with zero errors
- UX-04 verification gap is closed
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-foundation-design-system/02-04-SUMMARY.md`
</output>
