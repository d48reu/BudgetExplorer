---
phase: 02-app-foundation-design-system
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - budget-explorer-web/src/app/page.tsx
  - budget-explorer-web/src/components/homepage/HeroBanner.tsx
  - budget-explorer-web/src/components/homepage/QuickStats.tsx
  - budget-explorer-web/src/components/homepage/CTASection.tsx
  - budget-explorer-web/src/app/glossary/page.tsx
autonomous: false
requirements:
  - PAGE-01
  - UX-07

must_haves:
  truths:
    - "Homepage displays the $13.2B hero number with a count-up animation rolling from $0 to $13.2B over ~2 seconds"
    - "Quick stats row shows key figures: 9 strategic areas, 35 departments, total employees, and fiscal year"
    - "Dual CTAs appear side by side: primary 'Explore the Budget' and secondary 'Calculate Your Taxes'"
    - "Homepage communicates 'What am I looking at?' within 3 seconds — visitor understands this is a budget transparency tool"
    - "Glossary page lists budget terms alphabetically with plain-English definitions"
    - "Homepage data (total budget, department count, etc.) comes from the database, not hardcoded"
  artifacts:
    - path: "budget-explorer-web/src/components/homepage/HeroBanner.tsx"
      provides: "Hero section with $13.2B count-up animation"
      contains: "CountUp"
    - path: "budget-explorer-web/src/components/homepage/QuickStats.tsx"
      provides: "Key figures row below hero"
      contains: "strategic"
    - path: "budget-explorer-web/src/components/homepage/CTASection.tsx"
      provides: "Dual CTA buttons"
      contains: "Explore"
    - path: "budget-explorer-web/src/app/page.tsx"
      provides: "Homepage Server Component assembling hero, stats, CTAs with real database data"
      contains: "getCurrentFiscalYear"
    - path: "budget-explorer-web/src/app/glossary/page.tsx"
      provides: "Glossary page listing budget terms"
      contains: "GLOSSARY_TERMS"
  key_links:
    - from: "budget-explorer-web/src/app/page.tsx"
      to: "budget-explorer-web/src/lib/db/queries.ts"
      via: "Server Component data fetching"
      pattern: "import.*getCurrentFiscalYear|getQuickStats"
    - from: "budget-explorer-web/src/components/homepage/HeroBanner.tsx"
      to: "react-countup"
      via: "count-up animation library"
      pattern: "import CountUp"
    - from: "budget-explorer-web/src/app/glossary/page.tsx"
      to: "budget-explorer-web/src/lib/glossary.ts"
      via: "glossary term definitions"
      pattern: "import.*GLOSSARY_TERMS"
---

<objective>
Build the homepage shell with hero banner, quick stats, CTAs, and the glossary page.

Purpose: Deliver the signature first impression of the Budget Explorer — the $13.2B hero number with count-up animation, key stats, and clear calls to action. This is the "What am I looking at?" moment (UX-07). Also build the glossary page for SEO and jargon reference. Visualization placeholder slots are left for Phase 3.

Output: Complete homepage with real database data and a glossary page.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-foundation-design-system/02-RESEARCH.md
@.planning/phases/02-app-foundation-design-system/02-01-SUMMARY.md
@.planning/phases/02-app-foundation-design-system/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage components (HeroBanner, QuickStats, CTASection) and homepage assembly</name>
  <files>
    budget-explorer-web/src/components/homepage/HeroBanner.tsx
    budget-explorer-web/src/components/homepage/QuickStats.tsx
    budget-explorer-web/src/components/homepage/CTASection.tsx
    budget-explorer-web/src/app/page.tsx
  </files>
  <action>
**Step 1: HeroBanner component**

Create `src/components/homepage/HeroBanner.tsx`:
- `'use client'` directive (interactive count-up animation)
- Props: `totalBudgetCents: string` (BigInt cents as string from data access layer)
- Convert cents to billions: `Number(totalBudgetCents) / 100 / 1_000_000_000`
- Use `react-countup` `<CountUp>` component:
  - `end={totalBillions}`, `decimals={1}`, `duration={2}` (rolling from $0 to $13.2B over ~2 seconds per user decision)
  - `separator=","`, `prefix="$"`, `suffix="B"`
  - `enableScrollSpy`, `scrollSpyOnce` (animation triggers when scrolled into view)
- Layout:
  - Centered text, vertical flex column
  - Subheading above number: "Miami-Dade County FY 2025-26 Total Budget" in `text-text-secondary text-sm uppercase tracking-wider`
  - Hero number: `text-5xl md:text-7xl lg:text-8xl font-heading font-bold text-text-primary`
  - Tagline below: "See where your money goes." in `text-text-secondary text-lg md:text-xl`
  - Vertical padding: `py-16 md:py-24`
- Semantic HTML: `<section aria-label="Budget overview">`

Implementation matches 02-RESEARCH.md HeroBanner example.

**Step 2: QuickStats component**

Create `src/components/homepage/QuickStats.tsx`:
- Props: `strategicAreaCount: number`, `departmentCount: number`, `totalEmployees: number`, `fiscalYear: string`
- Display as a horizontal row of stat cards (use Card component from Plan 02)
- 4 stats:
  1. Strategic Areas: show count (9) with label "Strategic Areas"
  2. Departments: show count (35) with label "Departments"
  3. Employees: show total with label "County Employees" (format with commas)
  4. Fiscal Year: show label (FY 2025-26) with label "Budget Year"
- Layout: `grid grid-cols-2 md:grid-cols-4 gap-4 max-w-4xl mx-auto px-4`
- Each stat: number in `text-2xl md:text-3xl font-heading font-bold`, label in `text-text-secondary text-sm`
- Use the Card component with centered text alignment

**Step 3: CTASection component**

Create `src/components/homepage/CTASection.tsx`:
- Two buttons side by side using the Button component from Plan 02:
  1. Primary: "Explore the Budget" — links to /explorer (Button with `variant="primary"` and `href="/explorer"`)
  2. Secondary: "Calculate Your Taxes" — links to /calculator (Button with `variant="secondary"` and `href="/calculator"`)
- Layout: `flex flex-col sm:flex-row gap-4 justify-center items-center py-8`
- Both CTAs centered, stacked on mobile, side by side on sm+

Per user decision: dual CTAs side by side with primary "Explore the Budget" and secondary "Calculate Your Taxes".

**Step 4: Homepage assembly (page.tsx)**

Replace `src/app/page.tsx` with the real homepage Server Component:
- Import and call `getCurrentFiscalYear()` and `getQuickStats()` from the data access layer
- Render components in order:
  1. `<HeroBanner totalBudgetCents={fiscalYear.totalBudget} />`
  2. `<QuickStats strategicAreaCount={stats.strategicAreaCount} departmentCount={stats.departmentCount} totalEmployees={stats.totalEmployees} fiscalYear={stats.fiscalYear} />`
  3. `<CTASection />`
  4. Placeholder section for Phase 3 visualizations: `<section aria-label="Budget visualizations" className="py-section">` with a comment marking where treemap, penny viz, and revenue donut will go
- All data comes from the database via Server Component data fetching (no client-side API calls)
- Add SEO metadata: `export const metadata` with title and description specific to the homepage

Semantic HTML structure:
```
<main>
  <HeroBanner />      <!-- section: budget overview -->
  <QuickStats />      <!-- section: key figures -->
  <CTASection />      <!-- section: calls to action -->
  <section>           <!-- section: visualization placeholders for Phase 3 -->
</main>
```
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && pnpm run build 2>&1 | tail -10</automated>
    <manual>Visit http://localhost:3000 — hero number animates from $0 to $13.2B, quick stats show real data, CTAs are visible and clickable</manual>
  </verify>
  <done>HeroBanner shows $13.2B with count-up animation over 2 seconds. QuickStats row displays 9 strategic areas, 35 departments, employee count, and FY 2025-26. Dual CTAs render side by side. All data is fetched from the database via Server Component. Placeholder section exists for Phase 3 visualizations.</done>
</task>

<task type="auto">
  <name>Task 2: Glossary page with budget term definitions</name>
  <files>
    budget-explorer-web/src/app/glossary/page.tsx
  </files>
  <action>
Create `src/app/glossary/page.tsx`:

- Server Component (no 'use client' needed — static content)
- Import `GLOSSARY_TERMS` from `@/lib/glossary`
- Render an alphabetically sorted list of budget terms with their definitions
- Each term: `<dt>` with term name in `font-heading font-semibold text-lg`, `<dd>` with definition in `text-text-secondary`
- Use semantic HTML: `<dl>` (definition list) for the glossary
- Page header: "Budget Glossary" title with subtitle "Plain-English explanations of budget terms used on this site" — this is the "What am I looking at?" moment for this page (UX-07)
- Add Breadcrumbs: Home > Glossary
- Add SEO metadata: `export const metadata` with title "Budget Glossary | Miami-Dade Budget Explorer" and description

Layout:
- Max width container: `max-w-3xl mx-auto px-4 py-section`
- Terms separated by border-bottom for scanability
- Each term block has vertical padding

This page serves two purposes:
1. Reference for users encountering budget jargon
2. SEO value — budget term definitions are highly searchable content (per user decision: "good for SEO")
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && pnpm run build 2>&1 | tail -10</automated>
    <manual>Visit http://localhost:3000/glossary — verify all terms display with definitions, page has breadcrumbs, and semantic HTML is correct</manual>
  </verify>
  <done>Glossary page at /glossary lists 11+ budget terms alphabetically with plain-English definitions. Page uses semantic HTML (dl/dt/dd). Breadcrumbs show Home > Glossary. Page has unique title and meta description.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of homepage and design system</name>
  <files>budget-explorer-web/src/app/page.tsx</files>
  <action>
No code changes. This is a human verification checkpoint.

What was built: Complete homepage with hero banner ($13.2B count-up), quick stats, CTAs, responsive navigation (desktop navbar + mobile tab bar), footer with PDF link, glossary page, and Miami-Dade design system.

Start dev server: `cd budget-explorer-web && pnpm run dev`
Open http://localhost:3000 in browser.

**Desktop (1024px+ viewport):**
- Top navbar visible with "Miami-Dade Budget Explorer" logo and nav links
- Hero section: $13.2B number animates from $0 over ~2 seconds
- Quick stats row shows 4 cards: Strategic Areas (9), Departments (35), Employees, FY 2025-26
- Dual CTAs visible: "Explore the Budget" (filled) and "Calculate Your Taxes" (outlined)
- Footer at bottom with Budget in Brief PDF link, Related Resources, About section
- Bottom disclaimer visible

**Mobile (resize to 375px width):**
- Top navbar is hidden
- Bottom tab bar visible with 4 tabs (Home, Explorer, Calculator, Glossary)
- Active tab (Home) is highlighted
- Layout stacks vertically and is readable at 375px
- Footer is scrollable above the tab bar

**Glossary page (http://localhost:3000/glossary):**
- Page title "Budget Glossary" with subtitle
- Alphabetical list of budget terms with definitions
- Breadcrumbs show Home > Glossary

**Design system:**
- Cards have flat design with subtle borders (no drop shadows)
- Colors are neutral (white/gray base), brand colors not used in UI chrome
- Inter font used for headings, system font for body text
- Overall feel: clean, modern, "Mint.com for county budgets" vibe

Resume signal: Type "approved" if the homepage and design system look correct, or describe any issues to fix.
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && pnpm run build 2>&1 | tail -5</automated>
    <manual>Human visually verifies all checklist items above</manual>
  </verify>
  <done>Human has approved the visual design, responsive layout, and overall feel of the homepage, glossary, navigation, and design system</done>
</task>

</tasks>

<verification>
1. `pnpm run build` completes without errors
2. Homepage loads at / with hero banner, quick stats, CTAs
3. Hero number animates from $0 to $13.2B using react-countup
4. Quick stats show real data from PostgreSQL database
5. CTAs link to /explorer and /calculator
6. Glossary page loads at /glossary with term definitions
7. Responsive layout works: navbar on desktop, tab bar on mobile
8. Footer displays on both pages with PDF link
</verification>

<success_criteria>
- Homepage delivers the "What am I looking at?" moment: visitor sees $13.2B budget, understands it's a budget transparency tool, and has clear next actions (Explore or Calculate)
- All homepage data comes from the database (not hardcoded)
- Glossary page provides SEO-friendly budget term definitions
- Human verification confirms visual quality matches the "Mint.com for county budgets" / Linear/Notion aesthetic
- Responsive layout confirmed working on desktop and mobile (375px+)
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-foundation-design-system/02-03-SUMMARY.md`
</output>
