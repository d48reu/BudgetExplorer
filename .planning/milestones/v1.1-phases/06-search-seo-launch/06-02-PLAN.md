---
phase: 06-search-seo-launch
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - budget-explorer-web/src/app/layout.tsx
  - budget-explorer-web/src/app/opengraph-image.tsx
  - budget-explorer-web/src/app/department/[slug]/opengraph-image.tsx
  - budget-explorer-web/src/app/explorer/[area-slug]/opengraph-image.tsx
  - budget-explorer-web/src/app/sitemap.ts
  - budget-explorer-web/src/app/robots.ts
  - budget-explorer-web/src/app/not-found.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02

must_haves:
  truths:
    - "Every page type (homepage, explorer, area detail, department, calculator, glossary, search) has a unique title and meta description"
    - "Root OG image renders a branded card with 'Miami-Dade Budget Explorer' title, tagline, and budgetexplorer.miami branding"
    - "Department OG images render the department name dynamically from the database"
    - "Strategic area OG images render the area name dynamically from the database"
    - "sitemap.xml lists all static pages plus all 35 department pages and 9 strategic area pages, generated from the database"
    - "robots.txt allows all crawlers and references the sitemap at budgetexplorer.miami/sitemap.xml"
    - "Custom 404 page shows branded 'page not found' with links to homepage and explorer"
    - "metadataBase is set to https://budgetexplorer.miami in the root layout"
    - "Layout includes a TODO comment for Umami analytics"
  artifacts:
    - path: "budget-explorer-web/src/app/layout.tsx"
      provides: "Updated root metadata with metadataBase, enhanced openGraph, analytics TODO comment"
      contains: "budgetexplorer.miami"
    - path: "budget-explorer-web/src/app/opengraph-image.tsx"
      provides: "Root default OG image for all pages without a route-level override"
      contains: "ImageResponse"
    - path: "budget-explorer-web/src/app/department/[slug]/opengraph-image.tsx"
      provides: "Dynamic OG image with department name"
      contains: "ImageResponse"
    - path: "budget-explorer-web/src/app/explorer/[area-slug]/opengraph-image.tsx"
      provides: "Dynamic OG image with strategic area name"
      contains: "ImageResponse"
    - path: "budget-explorer-web/src/app/sitemap.ts"
      provides: "Dynamic sitemap querying all department and area slugs from DB"
      contains: "MetadataRoute.Sitemap"
    - path: "budget-explorer-web/src/app/robots.ts"
      provides: "robots.txt with sitemap reference to canonical domain"
      contains: "budgetexplorer.miami"
    - path: "budget-explorer-web/src/app/not-found.tsx"
      provides: "Custom 404 page with navigation links"
      contains: "404"
  key_links:
    - from: "budget-explorer-web/src/app/layout.tsx"
      to: "budget-explorer-web/src/lib/constants.ts"
      via: "CANONICAL_DOMAIN used for metadataBase"
      pattern: "CANONICAL_DOMAIN"
    - from: "budget-explorer-web/src/app/sitemap.ts"
      to: "budget-explorer-web/src/lib/prisma.ts"
      via: "Prisma client for querying department and area slugs"
      pattern: "prisma"
    - from: "budget-explorer-web/src/app/sitemap.ts"
      to: "budget-explorer-web/src/lib/constants.ts"
      via: "CANONICAL_DOMAIN for sitemap URLs"
      pattern: "CANONICAL_DOMAIN"
    - from: "budget-explorer-web/src/app/department/[slug]/opengraph-image.tsx"
      to: "budget-explorer-web/src/lib/db/queries.ts"
      via: "getDepartmentDetail() for department name"
      pattern: "getDepartmentDetail"
    - from: "budget-explorer-web/src/app/robots.ts"
      to: "budget-explorer-web/src/lib/constants.ts"
      via: "CANONICAL_DOMAIN for sitemap URL"
      pattern: "CANONICAL_DOMAIN"
---

<objective>
Add SEO metadata, Open Graph images, sitemap, robots.txt, custom 404 page, and canonical domain configuration. Every page type gets unique metadata. Dynamic OG images are generated for department and strategic area pages. The sitemap is built from database queries. The root layout is updated with metadataBase and an analytics TODO.

Purpose: Makes every page optimized for search engine discovery and social sharing. When someone shares a department page on social media, they see a branded card with the department name. Search engines can crawl the full sitemap of all 44+ pages.
Output: All SEO infrastructure in place. The app is ready for public launch.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-search-seo-launch/06-CONTEXT.md
@.planning/phases/06-search-seo-launch/06-RESEARCH.md
@.planning/phases/06-search-seo-launch/06-01-SUMMARY.md

<interfaces>
<!-- Existing types and contracts the executor must use. -->

From budget-explorer-web/src/lib/constants.ts (created in Plan 06-01):
```typescript
export const CANONICAL_DOMAIN = 'https://budgetexplorer.miami'
export const POPULAR_SEARCH_SUGGESTIONS: string[]
```

From budget-explorer-web/src/lib/db/queries.ts (existing):
```typescript
export async function getDepartmentDetail(slug: string): Promise<SerializedDepartmentDetail | null>
// Returns { name, slug, area: { name, color }, operatingBudget, ... }

export async function getAreaWithDepartments(areaSlug: string): Promise<{
  area: SerializedStrategicArea & { description: string | null; departmentCount: number }
  departments: SerializedDepartment[]
} | null>
```

From budget-explorer-web/src/lib/prisma.ts:
```typescript
const prisma: PrismaClient
export default prisma
```

From budget-explorer-web/src/app/layout.tsx (current root metadata -- WILL BE UPDATED):
```typescript
export const metadata: Metadata = {
  title: { default: 'Miami-Dade Budget Explorer', template: '%s | Miami-Dade Budget Explorer' },
  description: "See where your tax dollars go. Explore Miami-Dade County's $13.2 billion budget with interactive visualizations.",
  openGraph: { title: '...', description: '...', siteName: '...', locale: 'en_US', type: 'website' },
}
```

From budget-explorer-web/src/app/department/[slug]/page.tsx (existing generateStaticParams):
```typescript
export async function generateStaticParams() {
  const departments = await prisma.departments.findMany({ select: { slug: true } })
  return departments.map((dept) => ({ slug: dept.slug }))
}
// generateMetadata already exists -- produces unique title and description per department
```

From budget-explorer-web/src/app/explorer/[area-slug]/page.tsx (existing generateMetadata):
```typescript
export async function generateMetadata({ params }: PageProps): Promise<Metadata>
// Already produces unique title and description per strategic area
```

All page-level metadata already exists on these pages:
- / (homepage) -- static metadata in page.tsx
- /explorer -- static metadata in page.tsx
- /explorer/[area-slug] -- dynamic generateMetadata
- /department/[slug] -- dynamic generateMetadata
- /calculator -- static metadata in page.tsx
- /glossary -- static metadata in page.tsx
- /search -- static metadata in page.tsx (created in Plan 06-01)

What's MISSING and needs to be added in this plan:
- metadataBase on root layout (required for OG images to resolve correctly)
- OG images (root default + per-department + per-area)
- sitemap.ts
- robots.ts
- not-found.tsx
- Analytics TODO comment in layout
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout update, OG images, and custom 404 page</name>
  <files>
    budget-explorer-web/src/app/layout.tsx,
    budget-explorer-web/src/app/opengraph-image.tsx,
    budget-explorer-web/src/app/department/[slug]/opengraph-image.tsx,
    budget-explorer-web/src/app/explorer/[area-slug]/opengraph-image.tsx,
    budget-explorer-web/src/app/not-found.tsx
  </files>
  <action>
    **1. Update `budget-explorer-web/src/app/layout.tsx`:**

    Modify the existing root metadata export. Add `metadataBase` and enhance `openGraph`. Do NOT change any other part of the layout (keep Inter font, Navbar, MobileTabBar, Footer, body classes).

    Update the metadata object to:
    ```typescript
    import { CANONICAL_DOMAIN } from '@/lib/constants'

    export const metadata: Metadata = {
      metadataBase: new URL(CANONICAL_DOMAIN),
      title: {
        default: 'Miami-Dade Budget Explorer',
        template: '%s | Miami-Dade Budget Explorer',
      },
      description:
        "See where your tax dollars go. Explore Miami-Dade County's $13.2 billion budget with interactive visualizations.",
      openGraph: {
        title: 'Miami-Dade Budget Explorer',
        description: 'See where your tax dollars go.',
        siteName: 'Miami-Dade Budget Explorer',
        locale: 'en_US',
        type: 'website',
      },
    }
    ```

    The key change is `metadataBase: new URL(CANONICAL_DOMAIN)` -- this sets the base URL for all relative OG image URLs and canonical tags across the entire app. Next.js uses this to resolve `opengraph-image.tsx` paths into absolute URLs in meta tags.

    Add a TODO comment for analytics inside the `<body>` tag, just before the closing `</body>`:
    ```tsx
    {/* TODO: Add Umami analytics before public launch - self-hosted on Vercel, free tier */}
    ```

    **2. Create `budget-explorer-web/src/app/opengraph-image.tsx` (root default OG image):**

    This is the default OG image for all pages that don't have a route-level override (homepage, explorer, calculator, glossary, search).

    ```typescript
    import { ImageResponse } from 'next/og'

    export const alt = 'Miami-Dade Budget Explorer'
    export const size = { width: 1200, height: 630 }
    export const contentType = 'image/png'

    export default async function Image() {
      return new ImageResponse(
        (
          <div
            style={{
              width: '100%',
              height: '100%',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              backgroundColor: 'white',
              fontFamily: 'Inter, system-ui, sans-serif',
            }}
          >
            <div
              style={{
                fontSize: 64,
                fontWeight: 700,
                color: '#111827',
                textAlign: 'center',
                maxWidth: '80%',
              }}
            >
              Miami-Dade Budget Explorer
            </div>
            <div
              style={{
                fontSize: 28,
                color: '#6B7280',
                marginTop: 16,
              }}
            >
              See where your tax dollars go
            </div>
            <div
              style={{
                position: 'absolute',
                bottom: 40,
                fontSize: 18,
                color: '#9CA3AF',
              }}
            >
              budgetexplorer.miami
            </div>
          </div>
        ),
        { ...size }
      )
    }
    ```

    **Design per user decision:** Minimal text card design with white background, title prominently displayed, small branding at bottom.

    **3. Create `budget-explorer-web/src/app/department/[slug]/opengraph-image.tsx`:**

    Dynamic OG image per department. Uses `getDepartmentDetail()` to fetch the department name.

    ```typescript
    import { ImageResponse } from 'next/og'
    import { getDepartmentDetail } from '@/lib/db/queries'

    export const alt = 'Department Budget'
    export const size = { width: 1200, height: 630 }
    export const contentType = 'image/png'

    export default async function Image({
      params,
    }: {
      params: Promise<{ slug: string }>
    }) {
      const { slug } = await params
      const detail = await getDepartmentDetail(slug)
      const title = detail?.name ?? 'Department'
      const areaName = detail?.area?.name ?? ''

      return new ImageResponse(
        (
          <div
            style={{
              width: '100%',
              height: '100%',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              backgroundColor: 'white',
              fontFamily: 'Inter, system-ui, sans-serif',
            }}
          >
            {areaName && (
              <div
                style={{
                  fontSize: 22,
                  color: '#6B7280',
                  marginBottom: 12,
                  textTransform: 'uppercase',
                  letterSpacing: '0.05em',
                }}
              >
                {areaName}
              </div>
            )}
            <div
              style={{
                fontSize: 56,
                fontWeight: 700,
                color: '#111827',
                textAlign: 'center',
                maxWidth: '80%',
                lineHeight: 1.2,
              }}
            >
              {title}
            </div>
            <div
              style={{
                position: 'absolute',
                bottom: 40,
                fontSize: 18,
                color: '#9CA3AF',
              }}
            >
              Miami-Dade Budget Explorer
            </div>
          </div>
        ),
        { ...size }
      )
    }
    ```

    **Notes:**
    - `params` is `Promise<>` in Next.js 16+ (async params).
    - The parent page already has `generateStaticParams`, so Next.js will pre-render OG images for all 35 departments at build time.
    - Shows the strategic area name above the department name for context.

    **4. Create `budget-explorer-web/src/app/explorer/[area-slug]/opengraph-image.tsx`:**

    Dynamic OG image per strategic area.

    ```typescript
    import { ImageResponse } from 'next/og'
    import { getAreaWithDepartments } from '@/lib/db/queries'

    export const alt = 'Strategic Area Budget'
    export const size = { width: 1200, height: 630 }
    export const contentType = 'image/png'

    export default async function Image({
      params,
    }: {
      params: Promise<{ 'area-slug': string }>
    }) {
      const { 'area-slug': areaSlug } = await params
      const data = await getAreaWithDepartments(areaSlug)
      const title = data?.area?.name ?? 'Strategic Area'

      return new ImageResponse(
        (
          <div
            style={{
              width: '100%',
              height: '100%',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              backgroundColor: 'white',
              fontFamily: 'Inter, system-ui, sans-serif',
            }}
          >
            <div
              style={{
                fontSize: 22,
                color: '#6B7280',
                marginBottom: 12,
                textTransform: 'uppercase',
                letterSpacing: '0.05em',
              }}
            >
              Strategic Area
            </div>
            <div
              style={{
                fontSize: 56,
                fontWeight: 700,
                color: '#111827',
                textAlign: 'center',
                maxWidth: '80%',
                lineHeight: 1.2,
              }}
            >
              {title}
            </div>
            <div
              style={{
                position: 'absolute',
                bottom: 40,
                fontSize: 18,
                color: '#9CA3AF',
              }}
            >
              Miami-Dade Budget Explorer
            </div>
          </div>
        ),
        { ...size }
      )
    }
    ```

    **Note:** The param key is `area-slug` (with hyphen) matching the directory name `[area-slug]`.

    **5. Create `budget-explorer-web/src/app/not-found.tsx`:**

    Custom 404 page matching the project design system:

    ```typescript
    import Link from 'next/link'
    import type { Metadata } from 'next'

    export const metadata: Metadata = {
      title: 'Page Not Found',
    }

    export default function NotFound() {
      return (
        <div className="flex flex-col items-center justify-center min-h-[60vh] px-4 text-center">
          <h1 className="text-7xl font-heading font-bold text-text-primary">
            404
          </h1>
          <p className="mt-4 text-lg text-text-secondary max-w-md">
            This page doesn&apos;t exist in the budget. It may have been moved or
            the URL might be incorrect.
          </p>
          <div className="mt-8 flex gap-4">
            <Link
              href="/"
              className="rounded-lg bg-mdc-blue px-6 py-3 text-sm font-medium text-white hover:bg-mdc-blue/90 transition-colors"
            >
              Go Home
            </Link>
            <Link
              href="/explorer"
              className="rounded-lg border border-border px-6 py-3 text-sm font-medium text-text-primary hover:bg-surface-secondary transition-colors"
            >
              Explore Budget
            </Link>
          </div>
        </div>
      )
    }
    ```

    **Design notes:**
    - Uses existing design system tokens (font-heading, text-primary, text-secondary, mdc-blue, border, surface-secondary).
    - Primary CTA "Go Home" uses mdc-blue solid button. Secondary "Explore Budget" uses outline style.
    - min-h-[60vh] centers content vertically within the page (accounting for navbar/footer).
    - Friendly copy: "This page doesn't exist in the budget."
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && npx tsc --noEmit</automated>
  </verify>
  <done>Root layout updated with metadataBase and analytics TODO. Root OG image created (default for all pages). Dynamic OG images created for departments (35 pages) and strategic areas (9 pages). Custom 404 page with branded design and navigation links. All type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Sitemap, robots.txt, and final build verification</name>
  <files>
    budget-explorer-web/src/app/sitemap.ts,
    budget-explorer-web/src/app/robots.ts
  </files>
  <action>
    **1. Create `budget-explorer-web/src/app/sitemap.ts`:**

    Dynamic sitemap that queries all department and area slugs from the database:

    ```typescript
    import type { MetadataRoute } from 'next'
    import prisma from '@/lib/prisma'
    import { CANONICAL_DOMAIN } from '@/lib/constants'

    export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
      const [departments, areas] = await Promise.all([
        prisma.departments.findMany({ select: { slug: true } }),
        prisma.strategic_areas.findMany({ select: { slug: true } }),
      ])

      const staticPages: MetadataRoute.Sitemap = [
        {
          url: CANONICAL_DOMAIN,
          lastModified: new Date(),
          changeFrequency: 'monthly',
          priority: 1.0,
        },
        {
          url: `${CANONICAL_DOMAIN}/explorer`,
          lastModified: new Date(),
          changeFrequency: 'monthly',
          priority: 0.9,
        },
        {
          url: `${CANONICAL_DOMAIN}/calculator`,
          lastModified: new Date(),
          changeFrequency: 'yearly',
          priority: 0.7,
        },
        {
          url: `${CANONICAL_DOMAIN}/glossary`,
          lastModified: new Date(),
          changeFrequency: 'yearly',
          priority: 0.5,
        },
        {
          url: `${CANONICAL_DOMAIN}/search`,
          lastModified: new Date(),
          changeFrequency: 'yearly',
          priority: 0.4,
        },
      ]

      const departmentPages: MetadataRoute.Sitemap = departments.map(
        (dept) => ({
          url: `${CANONICAL_DOMAIN}/department/${dept.slug}`,
          lastModified: new Date(),
          changeFrequency: 'monthly' as const,
          priority: 0.8,
        })
      )

      const areaPages: MetadataRoute.Sitemap = areas.map((area) => ({
        url: `${CANONICAL_DOMAIN}/explorer/${area.slug}`,
        lastModified: new Date(),
        changeFrequency: 'monthly' as const,
        priority: 0.8,
      }))

      return [...staticPages, ...departmentPages, ...areaPages]
    }
    ```

    **Key details:**
    - Uses `CANONICAL_DOMAIN` constant (hardcoded `https://budgetexplorer.miami` per user decision -- NOT derived from request headers).
    - Static pages: homepage (1.0), explorer (0.9), department pages (0.8), area pages (0.8), calculator (0.7), glossary (0.5), search (0.4).
    - Department and area slugs are queried from the database at build time, ensuring the sitemap always matches what's in the DB.
    - The total sitemap will have: 5 static + 35 departments + 9 areas = 49 URLs.

    **2. Create `budget-explorer-web/src/app/robots.ts`:**

    ```typescript
    import type { MetadataRoute } from 'next'
    import { CANONICAL_DOMAIN } from '@/lib/constants'

    export default function robots(): MetadataRoute.Robots {
      return {
        rules: {
          userAgent: '*',
          allow: '/',
        },
        sitemap: `${CANONICAL_DOMAIN}/sitemap.xml`,
      }
    }
    ```

    **Key details:**
    - Allows all crawlers to access all pages (this is a public transparency tool -- no pages need to be hidden).
    - References sitemap at the canonical domain, not a relative URL.

    **3. Final build verification:**

    After creating both files, run a full build to verify everything works together:
    ```bash
    cd /home/d48reu/BudgetExplorer/budget-explorer-web && npx next build
    ```

    Check the build output for:
    - `/sitemap.xml` appears in the route list
    - `/robots.txt` appears in the route list
    - `/opengraph-image` routes appear for root, department/[slug], and explorer/[area-slug]
    - `/not-found` renders as a custom 404
    - All 35 department pages and 9 area pages are statically generated
    - No build errors or warnings
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>sitemap.ts generates 49 URLs (5 static + 35 departments + 9 areas) using the canonical domain. robots.ts allows all crawlers with sitemap reference. Full build succeeds with OG images, sitemap, robots, and 404 all present in the output.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes -- all types correct
2. `npx next build` succeeds -- all pages, OG images, sitemap, robots built
3. Check build output:
   - `/sitemap.xml` listed as a generated route
   - `/robots.txt` listed as a generated route
   - `/opengraph-image` route at root level
   - `/department/[slug]/opengraph-image` routes for all 35 departments
   - `/explorer/[area-slug]/opengraph-image` routes for all 9 areas
   - `/not-found` renders custom 404
4. Visit `/sitemap.xml` -- verify 49 URLs with correct canonical domain
5. Visit `/robots.txt` -- verify "Allow: /" and sitemap reference
6. Visit a non-existent URL (e.g., `/nonexistent`) -- see custom 404 with "Go Home" and "Explore Budget" buttons
7. Inspect `<meta>` tags on homepage -- verify og:image points to budgetexplorer.miami/opengraph-image
8. Inspect `<meta>` tags on /department/fire-rescue -- verify og:image points to budgetexplorer.miami/department/fire-rescue/opengraph-image
9. Verify every page type has a unique `<title>` tag:
   - Homepage: "Miami-Dade Budget Explorer"
   - Explorer: "Budget Explorer | Miami-Dade Budget Explorer"
   - Department: "{Name} - {Area} | Budget Explorer"
   - Calculator: "Tax Calculator | Miami-Dade Budget Explorer"
   - Glossary: "Glossary | Miami-Dade Budget Explorer"
   - Search: "Search | Miami-Dade Budget Explorer"
   - 404: "Page Not Found | Miami-Dade Budget Explorer"
10. Verify analytics TODO comment exists in layout.tsx body
</verification>

<success_criteria>
- metadataBase set to https://budgetexplorer.miami in root layout
- Root OG image: white background, "Miami-Dade Budget Explorer" title, tagline, domain branding
- Department OG images: dynamic name from DB, strategic area label, domain branding
- Strategic area OG images: dynamic name from DB, "Strategic Area" label, domain branding
- sitemap.ts queries DB for all 35 department + 9 area slugs, produces 49 URLs
- robots.ts allows all crawlers with sitemap reference at canonical domain
- Custom 404 with "This page doesn't exist in the budget" message and Go Home / Explore Budget buttons
- Analytics TODO comment in layout.tsx
- All page types have unique title + meta description (verified via build + meta tag inspection)
- Full build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-search-seo-launch/06-02-SUMMARY.md`
</output>
