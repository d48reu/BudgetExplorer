---
phase: 05-tax-calculator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - budget-explorer-web/src/types/budget.ts
  - budget-explorer-web/src/lib/db/queries.ts
  - budget-explorer-web/src/lib/tax-math.ts
  - budget-explorer-web/src/app/calculator/page.tsx
  - budget-explorer-web/src/components/calculator/TaxCalculator.tsx
  - budget-explorer-web/src/components/calculator/PropertyValueInput.tsx
  - budget-explorer-web/src/components/calculator/TaxSummaryHero.tsx
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - PAGE-05

must_haves:
  truths:
    - "User can visit /calculator and see a property value text input with $ prefix and 4 preset quick-pick buttons ($150K, $300K, $500K, $750K)"
    - "User can enter or select a value and immediately see a hero number showing estimated annual tax bill plus monthly equivalent"
    - "User can toggle a homestead exemption checkbox and see the tax bill recalculate instantly"
    - "Page shows a friendly empty state before any value is entered"
  artifacts:
    - path: "budget-explorer-web/src/lib/tax-math.ts"
      provides: "Pure tax calculation functions"
      exports: ["calculateTaxBreakdown", "applyHomesteadExemption", "calculateCountyAllocation"]
    - path: "budget-explorer-web/src/app/calculator/page.tsx"
      provides: "Server component fetching millage rates and strategic areas"
      min_lines: 20
    - path: "budget-explorer-web/src/components/calculator/TaxCalculator.tsx"
      provides: "Main client orchestrator with state management"
      contains: "'use client'"
    - path: "budget-explorer-web/src/components/calculator/PropertyValueInput.tsx"
      provides: "Dollar input with formatting and preset buttons"
      contains: "'use client'"
    - path: "budget-explorer-web/src/components/calculator/TaxSummaryHero.tsx"
      provides: "Hero number display with monthly equivalent"
  key_links:
    - from: "budget-explorer-web/src/app/calculator/page.tsx"
      to: "budget-explorer-web/src/lib/db/queries.ts"
      via: "getMillageRates() and getStrategicAreas() server-side fetch"
      pattern: "getMillageRates|getStrategicAreas"
    - from: "budget-explorer-web/src/components/calculator/TaxCalculator.tsx"
      to: "budget-explorer-web/src/lib/tax-math.ts"
      via: "calculateTaxBreakdown() called in useMemo"
      pattern: "calculateTaxBreakdown"
    - from: "budget-explorer-web/src/components/calculator/TaxCalculator.tsx"
      to: "budget-explorer-web/src/components/calculator/PropertyValueInput.tsx"
      via: "assessedValue state passed as prop, onChange callback"
      pattern: "PropertyValueInput"
---

<objective>
Create the tax calculator computation engine, data layer, and core page UI. Users will be able to enter a property assessed value (or select a preset), toggle homestead exemption, and see their estimated total tax bill as a big hero number with monthly equivalent.

Purpose: Establishes the tax math engine, data pipeline from database to client, and the interactive input experience that all visualization components depend on.
Output: Working /calculator page with input, hero number, and homestead toggle. Visualization sections (authority breakdown, county drill-down) are placeholder slots wired in Plan 02.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tax-calculator/05-CONTEXT.md
@.planning/phases/05-tax-calculator/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From budget-explorer-web/src/types/budget.ts (existing types to reference):
```typescript
export type SerializedStrategicArea = {
  id: number
  name: string
  slug: string
  color: string | null
  operatingBudget: string  // cents as string
  capitalBudget: string    // cents as string
  centsPerDollar: number | null
}
```

From budget-explorer-web/src/lib/db/queries.ts (existing query to reuse):
```typescript
export async function getStrategicAreas(): Promise<SerializedStrategicArea[]>
```

From budget-explorer-web/src/lib/format.ts (existing formatters):
```typescript
export function formatDollarsFull(cents: string | number): string
export function formatDollarsAbbreviated(cents: string | number): string
```

From budget-explorer-web/src/components/ui/Card.tsx:
```typescript
export function Card({ header, footer, children, className }: CardProps)
```

From budget-explorer-web/prisma/schema.prisma (millage_rates model):
```prisma
model millage_rates {
  id             Int          @id @default(autoincrement())
  fiscal_year_id Int
  authority      String       @db.VarChar(100)
  millage_rate   Decimal      @db.Decimal(8, 4)
  is_county      Boolean?     @default(true)
  display_order  Int?
  fiscal_years   fiscal_years @relation(...)
}
```

From budget-explorer-web/src/lib/nav-config.ts:
```typescript
// /calculator route already defined — page just needs to be created
{ label: 'Calculator', href: '/calculator', icon: '\u2261' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data layer and tax calculation engine</name>
  <files>
    budget-explorer-web/src/types/budget.ts,
    budget-explorer-web/src/lib/db/queries.ts,
    budget-explorer-web/src/lib/tax-math.ts
  </files>
  <behavior>
    - calculateTaxBreakdown(300000, false, sampleRates) returns array with correct taxAmount for each authority (taxableValue * millageRate / 1000, rounded to nearest cent)
    - calculateTaxBreakdown(300000, true, sampleRates) reduces taxable value by $50,000 before calculating (uses $250,000)
    - applyHomesteadExemption(300000) returns 250000
    - applyHomesteadExemption(40000) returns 0 (cannot go negative)
    - calculateTaxBreakdown returns percentage field summing to ~100% across all authorities
    - calculateCountyAllocation(countyTaxTotal, strategicAreas) splits county portion by centsPerDollar proportions
    - All tax amounts are rounded to nearest cent (2 decimal places)
    - Total of individual authority taxes equals sum of all authority taxAmount fields
  </behavior>
  <action>
    **1. Add SerializedMillageRate type to budget.ts:**
    ```typescript
    export type SerializedMillageRate = {
      id: number
      authority: string
      millageRate: number    // Prisma Decimal converted to JS number
      isCounty: boolean
      displayOrder: number
    }
    ```

    **2. Add getMillageRates() query to queries.ts:**
    Query `millage_rates` table for FY 2025-26, ordered by `display_order`. Convert Prisma Decimal to Number via `Number(rate.millage_rate)` or `.toNumber()`. Convert `is_county` (nullable Boolean) to definite boolean via `?? true`. Return `SerializedMillageRate[]`.

    CRITICAL: Prisma returns `millage_rate` as a `Prisma.Decimal` object, NOT a JavaScript number. You MUST explicitly convert: `Number(rate.millage_rate)`. Failure to convert causes "cannot serialize non-POJOs" runtime error.

    **3. Create tax-math.ts with pure functions:**

    Types needed (define in tax-math.ts or import from budget.ts):
    ```typescript
    type MillageRate = { authority: string; millageRate: number; isCounty: boolean; displayOrder: number }
    type TaxByAuthority = { authority: string; millageRate: number; taxAmount: number; percentage: number; isCounty: boolean }
    type StrategicAreaAllocation = { name: string; slug: string; color: string | null; dollarAmount: number; percentage: number }
    ```

    Functions:
    - `applyHomesteadExemption(assessedValue: number): number` — returns `Math.max(0, assessedValue - 50_000)` per user decision (simplified flat $50K reduction, NOT the two-tier Florida approach)
    - `calculateTaxBreakdown(assessedValue: number, homesteadExempt: boolean, rates: MillageRate[]): TaxByAuthority[]` — applies homestead if flagged, then for each rate: `taxAmount = Math.round(taxableValue * rate.millageRate / 1000 * 100) / 100`. Computes percentage of total for each.
    - `calculateCountyAllocation(countyTaxTotal: number, areas: { name: string; slug: string; color: string | null; centsPerDollar: number | null }[]): StrategicAreaAllocation[]` — splits county tax total by centsPerDollar proportions. Sum of centsPerDollar across all areas gives total cents. Each area's share = (centsPerDollar / totalCents) * countyTaxTotal.
    - `getTotalTax(breakdown: TaxByAuthority[]): number` — sums all taxAmount values.
    - `getCountyTotal(breakdown: TaxByAuthority[]): number` — sums taxAmount where isCounty is true.

    All functions are pure (no side effects, no React, no imports beyond types). Format only at display layer, never in these functions.
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && npx tsc --noEmit</automated>
  </verify>
  <done>SerializedMillageRate type exported from budget.ts. getMillageRates() query returns serialized millage rates for FY 2025-26. tax-math.ts exports all 5 pure functions with correct types. TypeScript compiles with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Calculator page shell, input component, and hero display</name>
  <files>
    budget-explorer-web/src/app/calculator/page.tsx,
    budget-explorer-web/src/components/calculator/TaxCalculator.tsx,
    budget-explorer-web/src/components/calculator/PropertyValueInput.tsx,
    budget-explorer-web/src/components/calculator/TaxSummaryHero.tsx
  </files>
  <action>
    **1. Create PropertyValueInput.tsx** (`'use client'`):
    - Text input with `$` prefix as a visual element (absolute positioned span to the left of input)
    - `inputMode="numeric"` for mobile numeric keyboard
    - Format on blur pattern (NOT while typing — avoids cursor-jumping per research pitfall 3): strip non-digits while typing, format with `toLocaleString('en-US')` on blur
    - 4 preset quick-pick buttons below input: $150K, $300K, $500K, $750K (values: 150000, 300000, 500000, 750000)
    - Preset buttons use secondary styling (border, not filled) with the selected one highlighted
    - Props: `value: number`, `onChange: (value: number) => void`
    - Soft guardrails per user decision: show a subtle info note for unusual values. Thresholds (Claude's discretion): below $50,000 → "This seems low for Miami-Dade — you may owe no tax with homestead exemption." Above $2,000,000 → "This is in the top range for Miami-Dade properties."
    - Note: guardrail text is informational only (not blocking), styled as `text-text-muted text-xs`

    **2. Create TaxSummaryHero.tsx:**
    - Big hero number: "Your estimated annual tax bill: $X,XXX" using Intl.NumberFormat for formatting (round to nearest dollar, not cent — official estimator does the same per research)
    - Monthly equivalent below: "$XXX/month" (annual / 12, rounded to nearest dollar)
    - When totalTax is 0 or assessedValue is 0, show nothing (parent hides this component)
    - Props: `totalTax: number` (dollars), `monthlyEquivalent: number` (dollars)
    - Use `font-heading font-bold text-3xl` for the big number, `text-text-secondary text-lg` for monthly

    **3. Create TaxCalculator.tsx** (`'use client'`):
    - Main orchestrator component receiving server-fetched data as props
    - Props: `rates: SerializedMillageRate[]`, `areas: SerializedStrategicArea[]`
    - State: `assessedValue: number` (default 0), `homesteadExempt: boolean` (default false)
    - Derived state via `useMemo`: call `calculateTaxBreakdown()` and `calculateCountyAllocation()` from tax-math.ts. NEVER store derived values in useState.
    - Sticky sidebar layout: CSS Grid with `lg:grid-cols-[360px_1fr]` — input panel on left (sticky top), results on right. Below `lg` breakpoint: single column with input on top.
    - Sticky input panel: `lg:sticky lg:top-6 lg:self-start` on the left column
    - Input panel contains: PropertyValueInput + homestead checkbox
    - Homestead checkbox: `<label>` with `<input type="checkbox">` + brief explanation text: "Florida homestead exemption reduces taxable value by $50,000 for primary residences"
    - Results panel contains: TaxSummaryHero (when value > 0), then placeholder divs for AuthorityBreakdown and CountyDrillDown (to be wired in Plan 02). Add comments: `{/* Authority breakdown — Plan 05-02 */}` and `{/* County drill-down — Plan 05-02 */}`
    - Empty state: when assessedValue is 0, show friendly message in results area: "Enter your property's assessed value to see your tax breakdown." with a subtle icon or illustration placeholder
    - Disclaimer footer at bottom of results: "This is an estimate based on FY 2025-26 millage rates. Actual taxes may vary based on municipality and special taxing districts." styled as `text-text-muted text-xs mt-8`

    **4. Create page.tsx** (server component):
    - Fetch `getMillageRates()` and `getStrategicAreas()` in parallel via `Promise.all`
    - Export metadata: `{ title: 'Tax Calculator | Budget Explorer', description: 'See how your property taxes fund Miami-Dade County services.' }`
    - Render page title "Property Tax Calculator" as h1
    - Render `<TaxCalculator rates={rates} areas={areas} />`
    - Wrap in standard page padding: `className="px-(--spacing-page) py-6"`
  </action>
  <verify>
    <automated>cd /home/d48reu/BudgetExplorer/budget-explorer-web && npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>/calculator page builds successfully. Page renders with input field, 4 preset buttons, homestead checkbox, and hero number. Entering a value or selecting a preset shows the estimated tax bill. Toggling homestead recalculates immediately. Empty state shows before any input. Sticky sidebar layout on desktop, stacked on mobile.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — all types are correct, no compilation errors
2. `npx next build` succeeds — /calculator page statically analyzable
3. Navigate to /calculator — page loads with input field and preset buttons
4. Enter $300,000 — hero shows estimated annual tax and monthly equivalent
5. Toggle homestead — number decreases (taxable value reduced by $50K)
6. Select $150K preset — value updates and recalculates
7. Clear input — empty state message appears
8. Resize to mobile — layout collapses from sidebar to stacked
</verification>

<success_criteria>
- /calculator page exists and builds without errors
- Property value input accepts numeric input with dollar formatting on blur
- 4 preset buttons ($150K, $300K, $500K, $750K) populate the input
- Homestead exemption checkbox reduces taxable value by $50,000
- Hero number shows correct total tax (all authorities summed) and monthly equivalent
- Layout is sticky sidebar on desktop, stacked on mobile
- Empty state displayed when no value entered
- Disclaimer text present at bottom of results
</success_criteria>

<output>
After completion, create `.planning/phases/05-tax-calculator/05-01-SUMMARY.md`
</output>
